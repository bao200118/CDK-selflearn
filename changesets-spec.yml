version: 0.2

phases:
  pre_build:
    commands:
      # - echo "Fetching SSM parameters..."
      # # Use AWS CLI to fetch parameters by path
      # - |
      #   PARAMS=$(aws ssm get-parameters-by-path \
      #     --path "/cdk-parameters" \
      #     --with-decryption \
      #     --query 'Parameters[].{Name:Name,Value:Value}' \
      #     --output json)
      # - echo "Exporting parameters as environment variables..."
      # - |
      #   for row in $(echo "${PARAMS}" | jq -c '.[]'); do
      #     NAME=$(echo $row | jq -r '.Name' | sed 's#.*/##') # Extract only the last part of the parameter name
      #     VALUE=$(echo $row | jq -r '.Value')
      #     echo "Exporting $NAME"
      #     export "$NAME=$VALUE" # Export as env variable for the build
      #   done
      - npm install
      - echo "Configuring AWS CLI..."
      - aws --version  # Verify AWS CLI is installed
      - echo "Configuring AWS CDK..."
      - npm run cdk --version  # Verify AWS CDK is installed
      - echo "Retrieving AWS Account ID..."
      - export AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query "Account" --output text)  # Dynamically get Account ID
      - npm run cdk bootstrap aws://$AWS_ACCOUNT_ID/$AWS_REGION
      - npm run cdk-diff -- -c stage=stag
